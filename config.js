/**
 * config.js — глобальные настройки игры «Лего-паззл».
 *
 * Палитра цветов задана однобуквенными кодами (Y/R/B/...) — те же коды
 * используются в массивах levels.js и в `data-bg` / `data-part` атрибутах DOM.
 */
window.GAME_CONFIG = {
  // Сколько слотов в нижнем ряду для временного хранения деталей
  INVENTORY_SLOTS: 12,

  // Палитра. КОД -> HEX. Добавляй сюда новые цвета по мере появления уровней.
  // На MVP-уровне используются только Y, R, B.
  COLORS: {
    Y: '#f5d142',  // yellow
    R: '#d63b3b',  // red
    B: '#1c1c1c',  // black
    W: '#f0f0f0',  // white
    G: '#5cb85c',  // green
    L: '#4a90e2',  // blue
    O: '#e8862c',  // orange
  },

  // Дефолтные настройки (применяются если в сейве не указано иное)
  enableSound: true,
  enableVibration: true,
};
